# 📤 GitHub руу push хийх командууд

## 1. Бүх өөрчлөлтийг stage хийх

```bash
git add .
```

## 2. Commit хийх

```bash
git commit -m "feat: Картын систем засварласан + карт худалдан авах систем нэмсэн

- Картын үлдэгдэл тооцох системийн race condition засварласан
- Карт худалдан авах хүсэлт систем нэмсэн (CardRequest model)
- Админ төлбөр баталгаажуулахад карт буцаан олгох логик нэмсэн
- 2 удаа амжилттай захиалга хийсэн бол нэмэлт 1 карт олгох (бонус)
- User profile хуудас дээр карт худалдан авах UI нэмсэн
- Admin хуудас дээр картын хүсэлт хэсэг нэмсэн
- Order card-уудын зураг томруулсан, border-radius стандартчилсан"
```

## 3. GitHub руу push хийх

```bash
git push origin main
```

## Эсвэл нэг мөрөнд:

```bash
git add . && git commit -m "feat: Картын систем засварласан + карт худалдан авах систем нэмсэн" && git push origin main
```

